generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Prisma multi-file schema — domain models under models/
// See models/README.md for the layer map.
