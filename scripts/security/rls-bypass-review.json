{
  "reviewed": {
    "packages/api/src/modules/appointment/appointment-public.service.ts|const appt = await this.prisma.withBypassRls((tx) =>": "Lookup by global appointment identifier precedes strict tenant transaction updates.",
    "packages/api/src/modules/appointment/appointment-public.service.ts|const branch = await this.prisma.withBypassRls((tx) =>": "Public customer lookup resolves branch orgId before tenant-scoped appointment queries.",
    "packages/api/src/modules/appointment/appointment-booking.service.ts|const branch = await this.prisma.withBypassRls((tx) =>": "Public booking resolves branch orgId before tenant-scoped appointment writes.",
    "packages/api/src/modules/appointment/appointment-slot.service.ts|const branch = await this.prisma.withBypassRls((tx) =>": "Public slot availability resolves branch orgId before tenant-scoped reads.",
    "packages/api/src/modules/appointment/appointment-reminder.service.ts|const appointments = await this.prisma.withBypassRls((tx) =>": "Scheduled reminder scan resolves appointments across tenants before per-org consent lookup.",
    "packages/api/src/modules/appointment/appointment-feature.guard.ts|const branch = await this.prisma.withBypassRls(async (tx) => {": "Public guard resolves branch orgId to verify appointment feature enablement before active tenant request context.",
    "packages/api/src/modules/notification/notification-twilio-webhook.service.ts|const notification = await this.prisma.withBypassRls((tx) =>": "Twilio delivery callbacks resolve notifications by provider message id before status updates.",
    "packages/api/src/modules/notification/notification-twilio-webhook.service.ts|await this.prisma.withBypassRls((tx) =>": "Twilio webhook handlers update notification delivery state outside tenant request context.",
    "packages/api/src/modules/notification/notification-twilio-webhook.service.ts|await this.prisma.withBypassRls(async (tx) => {": "Inbound STOP/START sync updates customer and active ticket consent by phone across tenants.",
    "packages/api/src/modules/support/support.service.ts|this.prisma.withBypassRls((tx) =>": "Platform support inbox reads tenant activity logs while triaging cross-org requests.",
    "packages/api/src/modules/support/support.service.ts|const assignee = await this.prisma.withBypassRls((tx) =>": "Platform operator assignment resolves users across internal org membership.",
    "packages/api/src/modules/platform-admin/platform-data.service.ts|const rows = await this.prisma.withBypassRls(async (tx) => {": "Platform export endpoint intentionally aggregates cross-tenant records for operators.",
    "packages/api/src/modules/platform-admin/platform-pulse.service.ts|const [ticketsToday, waitingNow, servingDistinct] = await this.prisma.withBypassRls(": "Platform pulse metrics require cross-tenant counting by design.",
    "packages/api/src/modules/platform-admin/platform-admins.controller.ts|const admins = await this.prisma.withBypassRls((tx) =>": "Platform admin CRUD operates on internal org users outside tenant request context.",
    "packages/api/src/modules/platform-admin/platform-admins.controller.ts|const admin = await this.prisma.withBypassRls((tx) =>": "Platform admin detail lookup by id across internal org membership.",
    "packages/api/src/modules/platform-admin/platform-admins.controller.ts|const existingInternal = await this.prisma.withBypassRls((tx) =>": "Platform admin creation checks email uniqueness in internal org.",
    "packages/api/src/modules/platform-admin/platform-admins.controller.ts|user = await this.prisma.withBypassRls((tx) =>": "Platform admin user provisioning links account to internal org.",
    "packages/api/src/modules/platform-admin/platform-admins.controller.ts|user = await this.prisma.withBypassRls(async (tx) => {": "Platform admin user+account creation runs under RLS bypass in internal org.",
    "packages/api/src/modules/platform-admin/platform-admins.controller.ts|const user = await this.prisma.withBypassRls((tx) =>": "Platform admin removal resolves user before org membership check.",
    "packages/api/src/modules/platform-admin/platform-admins.controller.ts|await this.prisma.withBypassRls((tx) => tx.user.delete({ where: { id } }));": "Platform admin deletion operates on internal org users.",
    "packages/api/src/modules/platform-admin/platform-admins.controller.ts|await this.prisma.withBypassRls((tx) => tx.user.delete": "Platform admin deletion operates on internal org users.",
    "packages/api/src/modules/ticket/ticket-public.service.ts|const resolved = await deps.prisma.withBypassRls(async (tx) => {": "Public ticket/visit tracking first resolves orgId from opaque token/id, then switches to withTenant.",
    "packages/api/src/modules/ticket/ticket-public.service.ts|const branch = await deps.prisma.withBypassRls((tx) =>": "Public display board resolves branch orgId before tenant-scoped queue reads.",
    "packages/api/src/modules/ticket/ticket-track-sse.service.ts|this.prisma.withBypassRls(async (tx) => {": "SSE tracking stream resolves ticket by public token before tenant-scoped publish context.",
    "packages/api/src/modules/ticket/ticket-customer-consent.service.ts|const ticket = await this.prisma.withBypassRls((tx) =>": "Track preference endpoint starts from global ticket ID and then uses withTenant for writes.",
    "packages/api/src/modules/ticket/ticket-compliance.service.ts|const affected = await this.prisma.withBypassRls((tx) => tx.ticket.count({ where }));": "Cross-tenant PII anonymization dry-run counts tickets past retention across all orgs.",
    "packages/api/src/modules/ticket/ticket-compliance.service.ts|const stale = await this.prisma.withBypassRls((tx) =>": "Compliance batch jobs (PII anonymization, stale expiry) scan tickets across tenants under scheduler control.",
    "packages/api/src/modules/ticket/ticket-compliance.service.ts|const branches = await this.prisma.withBypassRls(async (tx) =>": "Prior-session waiting cleanup enumerates branches org-wide (optional org filter) before per-tenant ticket updates.",
    "packages/api/src/modules/ticket/ticket-issuance.service.ts|const branch = await deps.prisma.withBypassRls((tx) =>": "Public/kiosk ticket issuance resolves branch orgId before tenant-scoped writes.",
    "packages/api/src/modules/service/service.service.ts|const branch = await this.prisma.withBypassRls((tx) =>": "Public service catalog resolves branch orgId before tenant-scoped service reads.",
    "packages/api/src/modules/queue/queue.service.ts|const branch = await this.prisma.withBypassRls((tx) =>": "Public queue listing resolves branch orgId before tenant-scoped queue reads.",
    "packages/api/src/modules/branch/branch.service.ts|return this.prisma.withBypassRls(async (tx) => {": "Public branch profile lookup by id without authenticated tenant context.",
    "packages/api/src/common/startup/startup-seeder.service.ts|const usersWithoutRole = await this.prisma.withBypassRls((tx) =>": "Startup backfill scans users across orgs to assign missing owner roles.",
    "packages/api/src/modules/auth/auth-login.service.ts|const usersWithEmail = await this.prisma.withBypassRls((tx) =>": "Login resolves users by email across tenants before org selection.",
    "packages/api/src/modules/auth/auth-login.service.ts|await this.prisma.withBypassRls((tx) =>": "Login flow updates last-login and session state outside tenant request context.",
    "packages/api/src/modules/auth/auth-login.service.ts|const user = await this.prisma.withBypassRls((tx) =>": "Login membership resolution reads user records before tenant context is established.",
    "packages/api/src/modules/auth/auth-password.service.ts|const user = await this.prisma.withBypassRls((tx) =>": "Password reset/change resolves user by email or id before tenant-scoped updates.",
    "packages/api/src/modules/auth/auth-password.service.ts|const target = await this.prisma.withBypassRls((tx) =>": "Admin password reset targets user by id across tenant boundaries.",
    "packages/api/src/modules/auth/auth-password.service.ts|await this.prisma.withBypassRls(async (tx) => {": "Password hash updates run under bypass when tenant context is not yet set.",
    "packages/api/src/modules/auth/auth-token.service.ts|const user = await this.prisma.withBypassRls((tx) =>": "Token refresh validates user membership before issuing tenant-scoped JWT.",
    "packages/api/src/modules/auth/auth-session.service.ts|const row = await this.prisma.withBypassRls((tx) =>": "Session profile resolves user and platform-operator flag without tenant context.",
    "packages/api/src/modules/auth/auth-verification.service.ts|await this.prisma.withBypassRls(async (tx) => {": "Email/phone verification updates user records before tenant context exists.",
    "packages/api/src/modules/auth/auth-impersonation.service.ts|const operator = await this.prisma.withBypassRls((tx) =>": "Impersonation validates platform operator user before tenant switch.",
    "packages/api/src/modules/auth/jwt.strategy.ts|this.prisma.withBypassRls((tx) =>": "JWT validation loads user and org membership on every authenticated request.",
    "packages/api/src/modules/auth/two-factor.service.ts|return this.prisma.withBypassRls(fn);": "2FA setup/verify reads and updates user TOTP state outside tenant context.",
    "packages/api/src/modules/support/platform-operator.guard.ts|const dbUser = await this.prisma.withBypassRls((tx) =>": "Platform operator guard resolves user org slug and 2FA state cross-tenant.",
    "packages/api/src/common/guards/display-auth.guard.ts|const device = await this.prisma.withBypassRls((tx) =>": "Display JWT auth resolves device by id before tenant-scoped lobby access.",
    "packages/api/src/modules/display/display.service.ts|const device = await this.prisma.withBypassRls((tx) =>": "Display pairing and device lifecycle updates run without tenant request context.",
    "packages/api/src/modules/display/display.service.ts|await this.prisma.withBypassRls((tx) =>": "Display device status updates during pairing bypass tenant RLS.",
    "packages/api/src/modules/display/display.service.ts|return this.prisma.withBypassRls((tx) =>": "Display device lookup by pairing code resolves org before tenant reads.",
    "packages/api/src/modules/realtime/realtime.service.ts|const device = await this.prisma.withBypassRls((tx) =>": "Centrifugo display token generation resolves device by id cross-tenant.",
    "packages/api/src/modules/realtime/realtime.service.ts|const isDisplayDevice = await this.prisma.withBypassRls((tx) =>": "Realtime subscribe auth checks display device membership by id.",
    "packages/api/src/modules/realtime/realtime.service.ts|await this.prisma.withBypassRls((tx) =>": "Display device last-seen heartbeat updates bypass tenant RLS.",
    "packages/api/src/modules/announcement/announcement.service.ts|const [totals, acknowledged] = await this.prisma.withBypassRls((tx) =>": "Platform operator compliance counts announcement user state across tenants.",
    "packages/api/src/modules/billing/billing.service.ts|const sub = await this.prisma.withBypassRls((tx) =>": "Stripe webhook handlers resolve subscriptions by Stripe ID before tenant-scoped updates.",
    "packages/api/src/modules/billing/billing.service.ts|const purchase = await this.prisma.withBypassRls((tx) =>": "Stripe SMS pack checkout resolves purchase by session id before tenant-scoped invoice writes.",
    "packages/api/src/modules/billing/sms-credit-purchase.service.ts|const existing = await this.prisma.withBypassRls((tx) =>": "Stripe webhook completion resolves SMS credit purchase by checkout session id.",
    "packages/api/src/modules/platform-admin/platform-health.service.ts|const [items, total] = await this.prisma.withBypassRls((tx) =>": "Platform health dashboard lists org health snapshots across all tenants.",
    "packages/api/src/modules/platform-admin/platform-tenants.controller.ts|const sub = await this.prisma.withBypassRls((tx) =>": "Platform tenant plan lookup reads subscription outside tenant request context.",
    "packages/api/src/modules/support/support.service.ts|const request = await this.prisma.withBypassRls((tx) =>": "Platform support inbox reads and updates support requests across tenants.",
    "packages/api/src/modules/support/support.service.ts|const [items, total] = await this.prisma.withBypassRls((tx) =>": "Platform support list aggregates requests across all organizations.",
    "packages/api/src/modules/support/support.service.ts|return this.prisma.withBypassRls((tx) =>": "Platform unread support badge counts requests across tenants.",
    "packages/api/src/modules/support/support.service.ts|const created = await this.prisma.withBypassRls((tx) =>": "Platform operator replies create support messages under cross-tenant bypass.",
    "packages/api/src/modules/support/support.service.ts|const updated = await this.prisma.withBypassRls((tx) =>": "Platform operator updates support request status across tenants.",
    "packages/api/src/modules/support/support.service.ts|const full = await this.prisma.withBypassRls((tx) =>": "Platform reply email notification loads request subject cross-tenant.",
    "packages/api/src/common/startup/startup-seeder.service.ts|await this.prisma.withBypassRls((tx) =>": "Startup backfill creates owner role assignments for users missing roles across orgs.",
    "packages/api/src/modules/auth/auth-login.service.ts|const roleAssignment = await this.prisma.withBypassRls((tx) =>": "Login and 2FA completion resolve primary role before tenant context is established.",
    "packages/api/src/modules/auth/auth-login.service.ts|const u2 = await this.prisma.withBypassRls((tx) =>": "2FA verification loads TOTP secrets and backup hashes outside tenant RLS.",
    "packages/api/src/modules/auth/auth-login.service.ts|await this.prisma.withBypassRls(": "2FA backup-code consumption and last-login updates run with explicit orgId before tenant JWT exists.",
    "packages/api/src/modules/auth/auth-password.service.ts|await this.prisma.withBypassRls(": "Password reset completion updates account, user, and reset token before tenant request context.",
    "packages/api/src/modules/auth/auth-verification.service.ts|await this.prisma.withBypassRls(": "Email verification marks token used and activates user before tenant context is established.",
    "packages/api/src/modules/auth/two-factor.service.ts|return this.prisma.withBypassRls(fn, { orgId });": "2FA secret and backup-hash writes set app.current_org_id via bypass options for RLS-aligned user updates.",
    "packages/api/src/modules/auth/auth-password.service.ts|const ownerAssignment = await this.prisma.withBypassRls((tx) =>": "Password reset eligibility checks owner role before sending reset email.",
    "packages/api/src/modules/billing/billing.service.ts|? await this.prisma.withBypassRls((tx) =>": "Stripe invoice webhooks resolve subscription by Stripe id before tenant-scoped billing writes.",
    "packages/api/src/modules/platform-admin/platform-tenants.controller.ts|let sub = await this.prisma.withBypassRls((tx) =>": "Platform tenant plan change reads subscription outside tenant request context.",
    "packages/api/src/modules/realtime/realtime.service.ts|const queue = await this.prisma.withBypassRls((tx) =>": "Centrifugo queue channel auth resolves queue orgId to match token org.",
    "packages/api/src/modules/service/service.service.ts|const subServices = await this.prisma.withBypassRls((tx) =>": "Public booking sub-service catalog has no tenant context; scoped by serviceId in handler.",
    "packages/api/src/modules/support/support.service.ts|await this.prisma.withBypassRls((tx) =>": "Platform support triage updates request read state and reply flags cross-tenant.",
    "packages/api/src/prisma/tenant-context.util.ts|return prisma.withBypassRls(callback);": "Shared helper delegates to PrismaService bypass for auth and public paths.",
    "packages/api/src/modules/display/display.service.ts|const existing = await this.prisma.withBypassRls((tx) =>": "Display claim code lookup maps screen code to device without active tenant context.",
    "packages/api/src/modules/display/display.service.ts|const device = await this.prisma.withBypassRls(": "Display screen fingerprint pairing updates device status bypassing active tenant RLS.",
    "packages/api/src/modules/display/display.service.ts|await this.prisma.withBypassRls(": "Display screen JWT refresh updates device status without established user tenant session.",
    "packages/api/src/modules/display/display.service.ts|return this.prisma.withBypassRls(": "Heartbeat status check from display device runs outside authenticated tenant dashboard session.",
    "packages/api/src/modules/ticket/visit-track-sse.service.ts|this.prisma.withBypassRls(async (tx) => {": "Public customer SSE connection resolves visit org scope via public token before stream setup.",
    "packages/api/src/modules/auth/auth-registration.service.ts|const result = await this.prisma.withBypassRls(": "Registration provisions organization, users, and credentials before a tenant session context is established.",
    "packages/api/src/modules/auth/auth-verification.service.ts|const matched = await this.prisma.withBypassRls(async (tx) => {": "Resolving email verification token before tenant context is established."
  }
}
