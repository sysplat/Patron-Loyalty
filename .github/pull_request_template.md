## Summary

<!-- What changed and why (1–3 sentences). -->

## Checklist

- [ ] Tests / typecheck pass for touched packages (`pnpm --filter …`)
- [ ] **Prefer squash-merge** when merging this PR into `main` (keeps history readable). Avoid force-pushing rewritten `main` history unless the whole team agreed and a backup exists.

## Risk / rollout

<!-- Anything deployers should know (migrations, env vars, feature flags). -->
